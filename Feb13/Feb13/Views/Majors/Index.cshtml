@model IEnumerable<Feb13.Models.Major>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.majorName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Faculty.facultyName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.majorName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Faculty.facultyName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ID_major }) |
            @Html.ActionLink("Details", "Details", new { id=item.ID_major }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ID_major })
        </td>
    </tr>
}

</table>
